# Node Environment
NODE_ENV=production

# Server Configuration
PORT=3000

# Rate Limiting
RATE_LIMIT_MAX=200

# CORS Configuration (comma-separated list of allowed origins)
# For n8n, set this to your n8n instance URL
ALLOWED_ORIGINS=*

# Optional: API Key for additional security (implement in server.js if needed)
# API_KEY=your-secret-api-key-here